//Declare a variable temperature with an initial value of 20. Reassign it to a string value "".
//twenty degrees Print the variable before and after reassignment to observe type coercion.

let temperature = 20 ;
let tem = "20";

console.log(typeof temperature);
console.log(typeof tem);